name: 'centro_psicologico'
version: '1.0.0'
config-version: 2

profile: 'centro_psicologico'

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  centro_psicologico:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: datamart
